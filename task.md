# Multi-Asset Repo

Per-asset backlogs live under `assets/<asset>/task.md`.

Examples:

- `assets/slv/task.md`
- `assets/qqq/task.md`
- `assets/nvda/task.md`
- `assets/tsla/task.md`

## Monitoring Layer

- [x] Add per-asset active status summaries and dashboards for all non-SLV assets. Performance: each active asset now has `active_status_summary.tsv` and `active_status.html`, so the preferred line and current lane role can be reviewed quickly.
- [x] Add per-asset monitor snapshots for all non-SLV assets. Performance: each active asset now has `monitor_snapshot.tsv`, which reduces the preferred line to a single current action row such as `selected_now`, `watchlist_wait`, `reference_only`, or `research_only`.
- [x] Add a repo-level monitor board for all non-SLV assets. Performance: `monitor_board.tsv` and `monitor_board.html` now summarize the current cross-asset stance in one place. In the current snapshot, `IWM` is `selected_now`, `XLE` and `NVDA` are `watchlist_wait`, `QQQ` and `TSLA` are `inactive_wait`, `SPY` is `reference_only`, and `TLT` is `research_only`.
- [x] Upgrade the repo-level monitor board into the main dashboard page. Performance: `monitor_board.html` now uses a `gold`-style layout with spotlight cards on top and a colored overview chart below, so it can act as the single primary HTML entry point instead of forcing day-to-day review through multiple pages.
- [x] Add a repo-level monitor priority view for all non-SLV assets. Performance: `monitor_priority.tsv` and `monitor_priority.html` now sort the same board into daily review order with a suggested next step per asset. The current priority stack is `IWM` first, then `NVDA` and `XLE`, then `QQQ` and `TSLA`, with `SPY` and `TLT` kept only for context or research.
- [x] Add a repo-level focus shortlist for all non-SLV assets. Performance: `monitor_focus.tsv` and `monitor_focus.html` now filter the priority stack down to the daily review shortlist only. In the current snapshot, the focus set is `IWM`, `NVDA`, and `XLE`.

## Cross-Asset Priority

- [x] Record the current first-round priority shortlist from the 20-asset batch. Performance: the current official top-three follow-up assets are `XLP`, `VT`, and `IJH`.
- [x] Save the selection rationale for the current shortlist. Performance: `XLP` is the cleanest classifier candidate so far with `test_bal_acc=0.6731` on `ret_60 + sma_gap_60 + above_200dma_flag`; `VT` is the most conservative high-balance line with `test_bal_acc=0.6337` and `test_positive_rate=0.1182`; `IJH` remains the strongest operator-style candidate with `combo_neg115_top_15pct avg_return=5.00%`, `hit_rate=81.48%`, and `trade_count=27`.
- [x] Record the current second-round priority shortlist from the thematic and macro batch. Performance: the current official top-five follow-up assets from the second 20-asset batch are `FXB`, `FXE`, `GLD`, `KRE`, and `SMH`.
- [x] Save the selection rationale for the second-round shortlist. Performance: `FXB` is the cleanest second-round classifier line with `test_bal_acc=0.6803` and `test_positive_rate=0.3606`; `FXE` is the next-best balanced macro line at `test_bal_acc=0.5777`; `GLD` remains attractive as a high-f1 macro benchmark with `test_f1=0.8193` and `test_bal_acc=0.5763`; `KRE` is the best surviving thematic risk line above `0.51` balance; `SMH` still looks worth a heavier follow-up despite a weaker `test_bal_acc=0.4954` because the baseline `test_f1=0.6349` and `11.70%` average return suggest there may be a cleaner filtered rule underneath.
- [x] Save the second-round heavy-batch ranking after safe parallel runs. Performance: `GLD` now leads the second-round heavy batch on the headline model score at `0.6810` with `ret_60_plus_sma_gap_60_interaction`, `test_bal_acc=0.6159`, and a usable forward rule `combo_neg115_top_20pct avg_return=3.99%` over `31` trades.
- [x] Record the standout heavy-batch classifier from the second round. Performance: `FXB` remains the cleanest low-frequency classifier with `sma_gap_120`, `test_bal_acc=0.8260`, and `test_positive_rate=0.1306`, though its current best forward rule is only `3` trades so it still needs more robustness checks.
- [x] Record the second-round heavy-batch caveats. Performance: `FXE` kept decent classifier balance at `test_bal_acc=0.6586` but its best forward rule is currently negative, `SMH` found a stronger filtered rule with `combo_fixed_0_49 avg_return=7.23%` across `29` trades despite weak classifier balance, and `KRE` stayed usable but secondary with `combo_top_20pct avg_return=2.45%` over `29` trades.
- [x] Publish the current global heavy-batch leaderboard across both rounds. Performance: the current top five by headline model score are `GLD`, `FXB`, `XLP`, `FXE`, and `IJH`, while the strongest rule-first alternates are `SMH`, `IJH`, `FXB`, `VT`, and `GLD`.
- [x] Save the current global follow-up interpretation. Performance: `GLD` is the most balanced all-around macro candidate, `FXB` is the cleanest sparse classifier but still under-sampled in forward rules, `XLP` is still the cleanest practical first-round classifier, `IJH` remains one of the best operator-style assets with `5.00%` average forward return over `27` trades, and `SMH` is now the most interesting upside rule-follow-up because its filtered entry line reached `7.23%` average forward return over `29` trades even though the raw classifier stayed weak.
- [x] Record the current third-round baseline shortlist from assets 40 through 60. Performance: the current top follow-up assets from the rates and international batch are `MUB`, `HYG`, `TIP`, `EMB`, and `JNK`.
- [x] Save the third-round baseline rationale and caveats. Performance: `MUB` is the cleanest third-round classifier so far with `test_bal_acc=0.7901`, `test_f1=0.5532`, and `test_positive_rate=0.2479`; `HYG` follows with `test_bal_acc=0.7111` and the best balanced high-yield baseline of the group; `TIP` stays interesting as a low-frequency line with `test_bal_acc=0.6696` and `test_positive_rate=0.1084`; `EMB` is a usable macro credit line at `0.5927`; `JNK` remains a sparse but positive-return candidate; the international slice mostly collapsed into high-positive-rate baselines, and `RSX` needs extra care because its data ends on `2022-03-03`.
- [x] Save the third-round heavy-batch ranking after running the top five in parallel. Performance: `EMB` now leads the third-round heavy batch on the headline score at `0.5611` with `ret_60_plus_sma_gap_60_plus_distance_to_252_high`, `test_bal_acc=0.6667`, and the least-bad forward rule `combo_top_12_5pct avg_return=0.44%` over `5` trades.
- [x] Record the main third-round heavy-batch caveat. Performance: the third-round baseline leaders degraded sharply under the heavier sweep; `JNK` and `HYG` kept some classifier structure but their best forward rules stayed negative, while `MUB` and `TIP` fell into effectively unusable ultra-sparse heavy-batch lines with only `1` forward trade each.
- [x] Record the current fourth-and-fifth-round baseline shortlist from assets 60 through 100. Performance: the current baseline leaders from the large-cap and crypto batch are `ETHE`, `PEP`, `META`, `ETH-USD`, and `KO`.
- [x] Save the fourth-and-fifth-round baseline caveats. Performance: `ETHE` is the cleanest raw balance line at `test_bal_acc=0.5544`, `PEP` edges next at `0.5202`, `META` still clears `0.5149`, and `ETH-USD` remains usable at `0.5107`; most of the large-cap equity slice collapsed into high-positive-rate baselines near `0.50`, `BITO` failed outright with `test_bal_acc=0.0000`, and `IBIT` plus `BITB` are too short to trust yet with only `217` and `215` total rows.
- [x] Save the fourth-and-fifth-round heavy-batch ranking after parallel follow-up. Performance: `PEP` now leads the large-cap and crypto heavy batch on headline score at `0.5673` with `above_200dma_flag`, `test_bal_acc=0.5912`, and a strong filtered rule `combo_fixed_0_49 avg_return=10.60%` over `8` trades; `META` follows at `0.5673` with `ret_60_plus_rolling_return_120`, but it stayed almost all-positive on test with `test_positive_rate=0.9979`.
- [x] Record the main fourth-and-fifth-round heavy-batch caveats. Performance: `ETHE` and `ETH-USD` both kept eye-catching forward returns, but each still leaned heavily toward degenerate positive-rate behavior with `test_positive_rate=0.9442` and `0.9737`; `KO` stayed usable as a slower operator-style line with `combo_top_17_5pct avg_return=2.25%` over `83` trades, but its headline score only reached `0.5311`.
- [x] Refresh the global heavy-batch leaderboard after adding the fourth-and-fifth-round survivors. Performance: the top five by headline model score still remain `GLD`, `FXB`, `XLP`, `FXE`, and `IJH`, while the new large-cap entrants slot in immediately behind that tier with `PEP` at `0.5673` and `META` at `0.5673`; the most interesting new rule-first alternate is `ETHE`, whose `combo_neg115_fixed_0_47` line averaged `21.86%` over `9` trades but still needs a much stricter robustness check before promotion.
- [x] Shrink the 100-asset universe into a final top 10 follow-up list. Performance: the current official top ten focus list is `GLD`, `XLP`, `IJH`, `FXB`, `SMH`, `PEP`, `VT`, `RSP`, `META`, and `ETHE`, saved in `final_top_10_summary.tsv`; `FXE` missed the cut despite a stronger headline score because its best forward rule stayed negative, while `KO` remains the first reserve because it traded cleanly but with a weaker classifier profile.
- [x] Run the first dedicated follow-up pass for the final top 10. Performance: `top10_followup_round1_summary.tsv` now records asset-specific model references, operator references, walk-forward averages, and recent activity for `GLD`, `XLP`, `IJH`, `FXB`, `SMH`, `PEP`, `VT`, `RSP`, `META`, and `ETHE`; the most complete current balance-first names remain `GLD`, `XLP`, `VT`, and `FXB`, while `SMH`, `PEP`, `META`, and `ETHE` keep more operator-heavy upside profiles.
- [x] Save the current dedicated follow-up interpretation before deeper feature work. Performance: `GLD` is the only top-10 asset that already looks fully live-like on the first pass, with the current threshold line still active on `2026-03-26`; `XLP`, `IJH`, `VT`, and `RSP` stayed cleaner on balance but are currently inactive; `FXB`, `SMH`, and `PEP` still look promising as selective watchlist lines; `META` remains active but too biased toward all-positive behavior; `ETHE` still looks more like a speculative operator overlay than a robust live line.
- [x] Run the second dedicated follow-up pass for all final top-10 assets. Performance: `top10_followup_round2_summary.tsv` now records the stronger round-two model references and operator overlays across all ten assets; the biggest balance-preserving upgrades came from `XLP` adding `sma_gap_120`, `PEP` adding `distance_to_252_high`, and `RSP` adding `above_200dma_flag`, while `GLD` improved again through `rolling_vol_60` and `neg_weight=1.15`.
- [x] Save the current second-round interpretation before any third-layer feature search. Performance: `GLD` remains the strongest overall second-round candidate with `round2_score=0.9939`; `XLP`, `FXB`, `PEP`, and `VT` remain the cleanest balance-first names even though several are currently inactive; `SMH` and `ETHE` became even more operator-heavy with outsized rule-level returns; `META` still improved on selective rules but remains too biased toward all-positive behavior; `IJH` and `RSP` stayed viable, but they improved less dramatically than the leaders.
- [x] Run a third-layer feature search for the current eight non-ETHE top-focus assets. Performance: `followup_round3_validation_summary.tsv`, `followup_round3_operator_summary.tsv`, and `followup_round3_decision_summary.tsv` now exist for `XLP`, `IJH`, `FXB`, `SMH`, `PEP`, `VT`, `RSP`, and `META`; `FXB` leads this slice at `round3_score=0.9120`, followed by `XLP=0.9044`, `PEP=0.9020`, and `VT=0.8924`.
- [x] Save the first round-three interpretation before any live-promotion decisions. Performance: `FXB` still looks like the cleanest sparse classifier but remains under-sampled with only `3` trades, `XLP` and `PEP` remain the cleanest practical balance-first names, `VT` improved through `distance_from_60d_low`, `SMH` still carries the strongest operator upside but remains highly one-sided even after a heavier `neg_weight`, `RSP` improved modestly through `slope_20`, and `META` improved guardrails via `close_location_20 + neg_weight=1.3` but still stayed too biased with `test_positive_rate=0.9979`.
- [x] Start the benchmark-aware round-four follow-up pass for the same eight focus assets. Performance: `followup_round4_*` now completes for `XLP`, `IJH`, `FXB`, `SMH`, `PEP`, `VT`, and `RSP`, while `META` remains unfinished after the interrupted batch; the current partial round-four table is saved in `top8_followup_round4_partial_summary.tsv` with `FXB=0.9133`, `PEP=0.9095`, `XLP=0.8939`, and `VT=0.8613` as the leading completed names.
